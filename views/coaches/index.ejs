<h2>Coaching Staff</h2>
<p>
  Here's a list of all the coaches in the system. You can check their roles,
  ages, and which clubs they belong to.
</p>
<p>
  Want to bring in a new strategist or assistant? Add a new coach to the
  database below.
</p>
<a href="/coaches/new">
  <button>Add New Coach</button>
</a>
<ul class="coachesList">
  <% coaches.forEach(coach => { %>
  <li>
    <%= coach.name %> — Role: <%= coach.role %> — Age: <%= coach.age %>
    <form action="/coaches/<%= coach.id %>/edit" method="get">
      <button type="submit" class="editBtn">Edit</button>
    </form>
    <form action="/coaches/<%= coach.id %>/delete" method="post">
      <button type="submit" class="deleteBtn">Delete</button>
    </form>
  </li>
  <% }) %>
</ul>
