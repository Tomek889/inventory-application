<h2>Football Clubs</h2>
<p>
  Explore all the clubs currently managed in the system. You can view each
  club's details, including its founding year, home stadium, and country.
</p>
<p>
  Want to add a new club? Click the button below and keep building the football
  world.
</p>
<a href="/clubs/new">
  <button>Add New Club</button>
</a>
<ul class="clubsList">
  <% clubs.forEach(club => { %>
  <li>
    <strong><%= club.name %></strong> — Founded Year: <%= club.founded_year %> —
    Stadium: <%= club.stadium_name %> — Country: <%= club.country %>
    <form action="/clubs/<%= club.id %>/edit" method="get">
      <button type="submit" class="editBtn">Edit</button>
    </form>
    <form action="/clubs/<%= club.id %>/delete" method="post">
      <button type="submit" class="deleteBtn">Delete</button>
    </form>
  </li>
  <% }) %>
</ul>
